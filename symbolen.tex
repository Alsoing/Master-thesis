% Maak een lijst van de gebruikte symbolen. Geef het symbool, naam en eenheid. Gebruik steeds SI-eenheden en gebruik de symbolen en namen zoals deze voorkomen in de hedendaagse literatuur en normen.
% De symbolen worden alfabetisch gerangschikt in opeenvolgende lijsten: kleine letters, hoofdletters, Griekse kleine letters, Griekse hoofdletters. Onderstaande tabel geeft het format dat kan ingevuld en uitgebreid worden.
% Wanneer het symbool een eerste maal in de tekst of in een formule wordt gebruikt, moet het symbool verklaard worden.

% Note that LaTeX will automatically sort symbols correctly for you, you only need to add new
% symbols below here.

\newglossaryentry{sym:R}{
    % \si, \unit, \ohm are commands from siunitx
    % See: https://ctan.mines-albi.fr/macros/latex/contrib/siunitx/siunitx.pdf
    name={\ensuremath{\si{R}}},
    description={Elektrische weerstand},
    unit={\ensuremath{\unit{\ohm}}},
    type=symbols
}
\newglossaryentry{sym:A}{
    name={\ensuremath{\si{A}}},
    description={Oppervlakte},
    unit={\ensuremath{\unit{\metre\squared}}},
    type=symbols
}
\newglossaryentry{sym:m}{
    name={\ensuremath{\si{m}}},
    description={Massa},
    unit={\ensuremath{\unit{\kilo\gram}}},
    type=symbols
}

% symbol list is automatically printed here (only used symbols are printed)
\printglossary[type=symbols,style=symbolsStyle]
